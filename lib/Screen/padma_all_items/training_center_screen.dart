import 'package:flutter/material.dart';

class TrainingCenterScreen extends StatelessWidget {
  const TrainingCenterScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Traling screen"),
      ),
    );;
  }
}
