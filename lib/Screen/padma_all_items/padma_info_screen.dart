import 'package:flutter/material.dart';

class PadmaInfoScreen extends StatelessWidget {
  const PadmaInfoScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Padma info screen"),
      ),
    );;
  }
}
