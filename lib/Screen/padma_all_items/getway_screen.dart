import 'package:flutter/material.dart';

class GetwayScreen extends StatelessWidget {
  const GetwayScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Getway screen"),
      ),
    );
  }
}
