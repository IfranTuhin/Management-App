import 'package:flutter/material.dart';

class PadmaNoticesScreen extends StatelessWidget {
  const PadmaNoticesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Padma notices screen"),
      ),
    );
  }
}
