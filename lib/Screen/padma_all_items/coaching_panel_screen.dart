import 'package:flutter/material.dart';

class CoachingPanelScreen extends StatelessWidget {
  const CoachingPanelScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Coaching screen"),
      ),
    );
  }
}
