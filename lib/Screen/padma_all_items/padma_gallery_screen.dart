import 'package:flutter/material.dart';

class PadmaGalleryScreen extends StatelessWidget {
  const PadmaGalleryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Padma Gallery"),
      ),
    );
  }
}
