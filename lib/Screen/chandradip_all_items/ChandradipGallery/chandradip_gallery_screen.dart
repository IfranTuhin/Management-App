import 'package:flutter/material.dart';

class ChandradipGalleryScreen extends StatelessWidget {
  const ChandradipGalleryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Padma Gallery"),
      ),
    );
  }
}
