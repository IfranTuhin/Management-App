import 'package:flutter/material.dart';

class CandradipNoticesScreen extends StatelessWidget {
  const CandradipNoticesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Chandradip notices screen"),
      ),
    );
  }
}
